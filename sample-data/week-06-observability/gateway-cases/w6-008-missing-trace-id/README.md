# w6-008-missing-trace-id - Missing trace ID is generated by gateway

## Purpose

Verifies gateway generates and stores a trace ID when caller does not provide one.

## Expected gateway result

```txt
status: SUCCEEDED
failureType: none
retryable: false
```

## Notes

This is a deterministic synthetic case. It should not call a real Gemini/provider API.
