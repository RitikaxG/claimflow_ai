# Reviewer notes

This represents the state after `ADDITIONAL_INFORMATION_RECEIVED` has cleared missing fields/evidence.

The agent should continue to policy lookup instead of repeating the information request.

Expected action: `RETRIEVE_POLICY_CLAUSES`.
